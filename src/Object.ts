export class BaseObject {
  constructor(
    protected _x: number,
    protected _y: number,
    protected _z: number,
  ) {
    // there's supposed to be more in here like hitboxes and stuff oops!
    // if it was supposed to be this empty I would just remove it
  }
}

